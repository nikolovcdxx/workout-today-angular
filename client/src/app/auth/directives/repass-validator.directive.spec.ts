import { RepassValidatorDirective } from './repass-validator.directive';

describe('RepassValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RepassValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
